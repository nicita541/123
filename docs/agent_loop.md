# Agent Loop

The runtime loop is `Think -> Plan -> Act -> Observe -> Verify -> Replan | Final`. MVP execution uses a deterministic planner and mock LLM adapter so the loop is testable without network access.

